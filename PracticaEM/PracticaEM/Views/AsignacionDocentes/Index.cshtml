@model IEnumerable<PracticaEM.Models.AsignacionDocente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Curso.Ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GrupoClase.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curso.Ano)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GrupoClase.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AsigDocenteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AsigDocenteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AsigDocenteId })
        </td>
    </tr>
}

</table>
